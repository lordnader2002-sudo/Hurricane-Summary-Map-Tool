{
  "name": "hurricane-summary-map-tool",
  "version": "1.0.0",
  "description": "Browser-based tool that overlays NOAA hurricane KMZ tracks on a map and auto-highlights company properties inside the cone or within a distance buffer.",
  "private": true,
  "scripts": {
    "smoke-test": "node scripts/smoke-test.js"
  },
  "devDependencies": {
    "@turf/turf": "^6.5.0",
    "jsdom": "^29.1.1",
    "jszip": "^3.10.1",
    "papaparse": "^5.5.3"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet-image": "^0.4.0"
  }
}
